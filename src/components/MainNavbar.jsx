import React from 'react'

export default function MainNavbar() {
    return (
        <nav style={{
            display: "flex",
            justifyContent: "center",
            gap: "10px"
        }
        }>
            nav
        </nav>
    )
}
